{
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.20.1"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "scripts": {
    "deploy": "npx hh run .\\scripts\\deploy.js",
    "deploy:l": "npx hh run .\\scripts\\deploy.js --network localhost",
    "deploy:g": "npx hh run .\\scripts\\deploy.js --network goerli",
    "deploy:s": "npx hh run .\\scripts\\deploy.js --network sepolia"
  }
}
